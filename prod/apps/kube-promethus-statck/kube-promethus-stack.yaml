apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: kube-prometheus-stack
  namespace: argocd
spec:
  destination:
    namespace: monitoring
    server: https://kubernetes.default.svc
  project: default  # Specify the appropriate Argo CD project
  source:
    repoURL: https://prometheus-community.github.io/helm-charts
    targetRevision: 50.0.0
    chart: kube-prometheus-stack
    values: |
      grafana:
        enabled: true
      prometheus:
        enabled: false
      alertmanager:
        enabled: false
      kube-state-metrics:
        enabled: false
      nodeExporter:
        enabled: false
      kubelet:
        enabled: false
      pushgateway:
        enabled: false
      # Disable other components as needed
  syncPolicy:
    automated: {}
  syncOptions:
    - CreateNamespace=true
